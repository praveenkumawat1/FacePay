import React, { useRef, useState } from "react";

// SlidePuzzleCaptcha: drag slider all the way to unlock (human verification)
// Usage:
// <SlidePuzzleCaptcha onVerify={() => { /* your code here */ }} />
export default function SlidePuzzleCaptcha({ onVerify, disabled }) {
  // ...rest of your code
}
